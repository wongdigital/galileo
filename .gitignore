# --- Sched-prose-bearing paths: never committed (legal posture, see README) ---
# Raw fetched schedule data (titles, times + Sched-authored descriptions)
data/events.json
data/meta.json
# App snapshot store, live-corpus test fixtures, enrichment batch intermediates
data/snapshots/
tests/fixtures/live/
data/batch-*/
*.batch-request.jsonl
*.batch-results.jsonl
# Enrichment compile working state: batch id/retry list, and the review bucket
# (which quotes rejected spans lifted from Sched prose).
data/.batch-state.json
data/.batch-retry.json
data/review-bucket.json

# --- Secrets / local config ---
.env
.env.*
*.local.json

# --- Node / build ---
node_modules/
dist/
dist-web/
out/
*.log
*.tsbuildinfo

# --- Capacitor generated web payload ---
ios/App/App/public/
ios/App/App/capacitor.config.json

# --- OS noise ---
.DS_Store
docs/inspo/

# --- Local backup snapshots (never committed) ---
backups/
